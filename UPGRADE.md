# Upgrade

This file documents breaking changes between major versions. The SDK follows [semantic versioning](https://semver.org/);
breaking changes only happen between major versions.

There are no upgrades to document yet — `1.x` is the first release line.
